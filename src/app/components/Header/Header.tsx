"use client";
import { HeaderStyle } from "./Header.css";

const Header = () => {
  return (
    <header className={HeaderStyle}>
      <h1> Styled Header</h1>
    </header>
  );
}

export default Header;